let channel: string = 'Gaveta';
let subscribers: number = 610234;

// channel = subscribers;
console.log(channel);

// let nome: string = 'Teste'

function sum(a: any, b: any) {
  return a + b;
}

let anything;
anything = 123;
anything = 'abc';

function hi(isMorning: boolean): string {
  let salute: string;

  if (isMorning) salute = 'Good Morning!'
  else salute = 'Good Day!'

  return salute;
}